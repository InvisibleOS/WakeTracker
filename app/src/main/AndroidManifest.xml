<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="true" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

        <application
        android:allowBackup="true"
        android:icon="@mipmap/alarm"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/alarm_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WakeTracker"
        tools:targetApi="31">

        <property
            android:name="android.data_extraction_rules"
            android:resource="@xml/data_extraction_rules" />
        <property
            android:name="android.full_backup_content"
            android:resource="@xml/backup_rules" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.WakeTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

            <activity
                android:name=".NfcScanActivity"
                android:theme="@style/Theme.TransparentNoAnimation"

                android:launchMode="singleInstance"
                android:taskAffinity=""
                android:excludeFromRecents="true"
                android:noHistory="true"

                android:exported="true">
                <intent-filter android:autoVerify="true">
                    <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                    <action android:name="android.intent.action.VIEW" />
                    <category android:name="android.intent.category.DEFAULT" />
                    <category android:name="android.intent.category.BROWSABLE" />
                    <data android:scheme="waketracker" android:host="scan" />
                </intent-filter>
            </activity>

    </application>
</manifest>